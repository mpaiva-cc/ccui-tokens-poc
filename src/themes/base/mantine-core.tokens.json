{
  "$description": "System-level configuration defaults for global rendering behavior, cursor style, font smoothing, and z-index layering.",
  "system": {
    "$description": "Global system configuration tokens that control rendering defaults across all themes.",
    "scale": {
      "$value": 1,
      "$type": "number",
      "$description": "Global scale factor. 1 = default size, 0.8 = 80% size, 1.2 = 120% size."
    },
    "cursorType": {
      "$value": "pointer",
      "$type": "string",
      "$description": "Cursor style for interactive elements. 'pointer' for clickable, 'default' for standard."
    },
    "webkitFontSmoothing": {
      "$value": "antialiased",
      "$type": "string",
      "$description": "WebKit font smoothing. 'antialiased' for crisp text on macOS/iOS."
    },
    "mozFontSmoothing": {
      "$value": "grayscale",
      "$type": "string",
      "$description": "Mozilla font smoothing. 'grayscale' for crisp text on Firefox."
    },
    "headingTextWrap": {
      "$value": "wrap",
      "$type": "string",
      "$description": "Text wrapping behavior for headings. 'wrap' allows line breaks, 'balance' balances lines."
    },
    "zIndex": {
      "$description": "Z-index stacking layers for UI elevation ordering.",
      "app": {
        "$value": 100,
        "$type": "number",
        "$description": "Base app layer z-index."
      },
      "modal": {
        "$value": 200,
        "$type": "number",
        "$description": "Modal dialog z-index."
      },
      "popover": {
        "$value": 300,
        "$type": "number",
        "$description": "Popover/dropdown z-index."
      },
      "overlay": {
        "$value": 400,
        "$type": "number",
        "$description": "Overlay/backdrop z-index."
      },
      "max": {
        "$value": 9999,
        "$type": "number",
        "$description": "Maximum z-index for critical elements."
      }
    }
  }
}
