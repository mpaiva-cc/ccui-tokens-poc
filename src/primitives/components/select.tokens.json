{
    "$description": "Select/dropdown component tokens. Selects allow users to choose one option from a list displayed in a dropdown menu. These tokens compose primitive tokens into select-specific configurations. Use selects for: single-choice forms, filter dropdowns, settings selection, sort options. ACCESSIBILITY: Ensure keyboard navigation (arrow keys, type-ahead), proper ARIA attributes (combobox role), and visible focus states.",
    "select": {
        "height": {
            "$description": "Select input height values. Heights match input field sizing for form consistency.",
            "xs": {
                "$value": "{sizing.input.xs}",
                "$description": "Extra small select height (30px). Use for: compact forms, inline selects, space-constrained UIs. Matches xs input height."
            },
            "sm": {
                "$value": "{sizing.input.sm}",
                "$description": "Small select height (36px). Use for: standard density forms, filter bars, secondary selects. Matches sm input height."
            },
            "md": {
                "$value": "{sizing.input.md}",
                "$description": "Medium select height (42px). DEFAULT. Use for: most form selects, settings, primary data entry. Matches md input height."
            },
            "lg": {
                "$value": "{sizing.input.lg}",
                "$description": "Large select height (48px). Use for: touch-optimized forms, prominent selects, hero sections. Matches lg input height."
            },
            "xl": {
                "$value": "{sizing.input.xl}",
                "$description": "Extra large select height (60px). Use for: high-emphasis selects, marketing forms, maximum touch targets. Matches xl input height."
            }
        },
        "padding": {
            "$type": "dimension",
            "$description": "Select input horizontal padding. Controls spacing between text and borders/icon.",
            "x": {
                "xs": {
                    "$value": "{spacing.xs}",
                    "$description": "Extra small horizontal select padding (10px). Compact padding for xs selects. Leaves room for dropdown icon."
                },
                "sm": {
                    "$value": "{spacing.sm}",
                    "$description": "Small horizontal select padding (12px). Standard padding for sm selects and efficient layouts."
                },
                "md": {
                    "$value": "{spacing.sm}",
                    "$description": "Medium horizontal select padding (12px). DEFAULT. Same as sm - compact but comfortable for most use cases."
                },
                "lg": {
                    "$value": "{spacing.md}",
                    "$description": "Large horizontal select padding (16px). More generous padding for lg selects with better text breathing room."
                },
                "xl": {
                    "$value": "{spacing.md}",
                    "$description": "Extra large horizontal select padding (16px). Same as lg - comfortable padding for xl selects."
                }
            }
        },
        "borderRadius": {
            "$value": "{radius.sm}",
            "$type": "dimension",
            "$description": "Select input border radius (4px). Matches standard input field radius for form consistency."
        },
        "borderWidth": {
            "$value": "{borderWidth.thin}",
            "$type": "dimension",
            "$description": "Select input border width (1px). Standard form control border. Clear boundary without visual heaviness."
        },
        "fontSize": {
            "$description": "Select text font sizes. Should match input field font sizes for form consistency.",
            "xs": {
                "$value": "{typography.fontSize.xs}",
                "$description": "Extra small select font size (12px). Compact text for xs selects. Use for dense forms or secondary controls."
            },
            "sm": {
                "$value": "{typography.fontSize.sm}",
                "$description": "Small select font size (14px). Standard text for most select inputs. Readable and space-efficient."
            },
            "md": {
                "$value": "{typography.fontSize.md}",
                "$description": "Medium select font size (16px). DEFAULT. Body text size for comfortable reading. Most common for primary forms."
            },
            "lg": {
                "$value": "{typography.fontSize.md}",
                "$description": "Large select font size (16px). Same as md - maintains readability without growing too large."
            },
            "xl": {
                "$value": "{typography.fontSize.lg}",
                "$description": "Extra large select font size (18px). Larger text for prominent selects or accessibility focus."
            }
        },
        "dropdown": {
            "$description": "Select dropdown menu configuration. The dropdown appears below (or above) the select trigger.",
            "borderRadius": {
                "$value": "{radius.sm}",
                "$type": "dimension",
                "$description": "Dropdown menu border radius (4px). Matches select input radius. Creates consistent, connected feel when open."
            },
            "shadow": {
                "$value": "{shadow.md}",
                "$description": "Dropdown menu shadow. Medium shadow provides elevation and separation from page content. Indicates floating layer."
            },
            "padding": {
                "$value": "{spacing.xs}",
                "$type": "dimension",
                "$description": "Dropdown menu inner padding (10px). Small padding around the options list. Options have their own padding."
            },
            "maxHeight": {
                "$value": "{sizing.select.dropdownMaxHeight}",
                "$type": "dimension",
                "$description": "Dropdown menu maximum height (300px). Limits dropdown size for long option lists. Enables scrolling when exceeded."
            },
            "zIndex": {
                "$value": "{zIndex.popover}",
                "$description": "Dropdown z-index stacking level (200). Above most page content but below modals. Standard popover layer."
            }
        },
        "option": {
            "$description": "Select option (menu item) configuration. Options are clickable items in the dropdown list.",
            "padding": {
                "x": {
                    "$value": "{spacing.sm}",
                    "$type": "dimension",
                    "$description": "Option horizontal padding (12px). Comfortable padding for option text. Wider click/tap target than text alone."
                },
                "y": {
                    "$value": "{spacing.xs}",
                    "$type": "dimension",
                    "$description": "Option vertical padding (10px). Creates comfortable row height for options. Good for keyboard navigation and touch."
                }
            },
            "borderRadius": {
                "$value": "{radius.xs}",
                "$type": "dimension",
                "$description": "Option border radius (2px). Subtle rounding for hover/selected state backgrounds. Creates soft highlight effect."
            }
        },
        "transition": {
            "$description": "Select interaction transition configuration.",
            "duration": {
                "$value": "{motion.duration.fast}",
                "$description": "Select transition duration (100ms). Fast transitions for dropdown open/close and option hover states."
            },
            "easing": {
                "$value": "{motion.easing.easeOut}",
                "$description": "Select transition easing (ease-out). Smooth deceleration for dropdown appearance and state changes."
            }
        },
        "icon": {
            "$description": "Select dropdown indicator icon (chevron) configuration.",
            "size": {
                "$value": "{sizing.select.iconSize}",
                "$type": "dimension",
                "$description": "Select chevron icon size (16px). Dropdown indicator arrow. Sized to be visible but not dominant."
            },
            "spacing": {
                "$value": "{spacing.xs}",
                "$type": "dimension",
                "$description": "Spacing between text and icon (10px). Separates selected text from chevron indicator."
            }
        }
    }
}
